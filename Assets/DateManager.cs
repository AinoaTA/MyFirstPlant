using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DateManager : MonoBehaviour
{
    // The plant says something, you reply with something. Each reply has a tag
    // If tags match what the other said, gain points. If you say something she dislikes, lose a point
    // The match points are counted in the end and if its above the character "date" threshold then good.
    // If there's not enough points, then lose.
}
